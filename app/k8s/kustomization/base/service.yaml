apiVersion: v1
kind: Service
metadata:
  name: telegram-support-bot
  labels:
    app: telegram-support-bot
spec:
  selector:
    app: telegram-support-bot
  ports:
    - name: http
      port: 8080
      targetPort: 8080
